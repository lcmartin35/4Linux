git init
git status
git add <nome dos arquivos ou .< para tudo>
git commit -m 'mensagem'
git push -u origin master

# trocar usuario

git config --global user.name "John Doe"
git config --global user.email johndoe@example.com
